/*
Requirements:
Write a function compare which returns the boolean result of its two 
parameters by checking that they have equal value and equal type.

Use the following provided code to begin:

function compare(x, y) {
    
}
*/

function compare(x, y){
    return (x === y);
}
